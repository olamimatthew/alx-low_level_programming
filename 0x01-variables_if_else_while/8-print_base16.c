#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Prints the digits of base 16
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 28; a < 38; a++)
	{
		putchar(a);
	}
	for (a = 67; a < 73; a++)
	{
		putchar(a);
	}

	putchar('\n');
	return (0);
}

