#include "main.h"

/**
 * _islower - Shows 1 if the input is a lowercase character. If another case shows 0
 *
 * Return: 1 for lowercase character, 0 otherwise
 */

int _islower(int c)
{
	if (c >= 'a'; c <= 'z';)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}
