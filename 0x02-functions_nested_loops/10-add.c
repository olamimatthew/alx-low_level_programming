#include "main.h"

/**
 * add - adds two numbers together and returns the result
 *
 * @i: number one
 * @j: number two
 *
 * Return: returns the result
 */
int add(int i, int j)
{
	return (i + j);
}
