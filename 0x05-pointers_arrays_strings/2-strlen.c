#include "main.h"

/**
 * _strlen - returns the lenght of a string
 * @s: input string
 * Return: Learn of a string.
 */

void swap_int(int *a, int *b)
{
	int count = 0;

	while (*(s + count) != '\0')
		count++;
	return (count;
}
