#include "main.h"

/**
 * swap_int - swaps the value of two intergers between themselves
 * @a: first interger
 * 2b: second interger
 */

void swap_int(int *a, int *b);
{
	int c = *a;
	*a = *b;
	*b = c;
}
